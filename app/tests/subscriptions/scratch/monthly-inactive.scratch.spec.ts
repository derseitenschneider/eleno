import test, { expect } from '@playwright/test'
import { SubscriptionPOM } from '../../pom/SubscriptionPOM'

test('can deactivate and activate subscription', async ({ page }) => {
  console.log('inactive monthly')
})
