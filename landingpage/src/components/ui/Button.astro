---
interface Props {
  type: 'primary' | 'secondary';
  link: string;
  target?: string;
}

const { type, link, target } = Astro.props;
---

<a href={link} class={`button button--${type}`} target={target}>
  <slot />
</a>

<style lang="scss">
  .button {
    display: block;
    width: fit-content;
    padding: 10px 16px;
    border-radius: 8px;
    font-weight: 700;
    font-size: 1.8rem;
    transition: all 0.45s ease;

    &--primary:link,
    &--primary:visited,
    &--primary:active {
      background-color: var(--clr-primary-600);
      border: 1px solid var(--clr-primary-600);

      color: #fff;
    }
    &--primary:hover {
      background-color: var(--clr-primary-800);
      border: 1px solid var(--clr-primary-800);
      box-shadow: var(--box-shadow);
      transform: translateY(-2px);
    }

    &--secondary:link,
    &--secondary:visited,
    &--secondary:active {
      border: 1px solid var(--clr-primary-600);

      color: var(--clr-primary-600);
    }
    &--secondary:hover {
      border: 1px solid var(--clr-primary-800);
      color: var(--clr-primary-800);
      box-shadow: var(--box-shadow);
      transform: translateY(-2px);
    }
  }
</style>
